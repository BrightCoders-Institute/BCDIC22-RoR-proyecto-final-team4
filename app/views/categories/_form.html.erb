<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="content-new-category">
    <div>
      <%= form.text_field :namecategory, class: "input-new-category", placeholder: "Name Cartegory" %>
    </div>
    <div>
      <%= form.submit class: "bottom-new-category"%>
    </div>
    <div class="bottum-back">
      <%= link_to "Back", categories_path , class: "buttom-back-categories"%>
    </div>
  </div>
<% end %>
